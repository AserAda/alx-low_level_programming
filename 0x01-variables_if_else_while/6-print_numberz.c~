#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main- will show if greater than 5, less than 6 or 0
 * Return: will returns 0
 */
int main(void)
{
int i;
for (i = 48; i < 58; i++)
{
putchar(i);
}
putchar('\n');
return (0);
}
